apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: rating-db
  namespace: dev
  labels:
    app: mongo
spec:
  serviceName: mongo-rating-svc
  replicas: 1
  selector:
    matchLabels:
      role: rating-db
  template:
    metadata:
      labels:
        role: rating-db
    spec:
      containers:
        - name: rating-mongodb
          image: mongo:latest
          ports:
            - containerPort: 27017
          envFrom:
            - configMapRef:
                name: rating-db-config
            - secretRef:
                name: rating-db-secret
          volumeMounts:
            - name: rating-mongo-persistent-storage
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: rating-mongo-persistent-storage
      spec:
        storageClassName: rating-mongodb-storage
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi